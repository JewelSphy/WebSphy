# Optional visitor tracking (dashboards are private — only you see the numbers)

# Google Analytics 4 — https://analytics.google.com → Admin → Data streams → Web
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Cloudflare Web Analytics — Cloudflare → Analytics & logs → Web analytics → Add site
# NEXT_PUBLIC_CF_BEACON_TOKEN=your-beacon-token

# Also enable Vercel Analytics in your Vercel project → Analytics tab (already wired in the code).

# --- Contact form ---
WEB3FORMS_ACCESS_KEY=

# --- Option B (Resend) ---
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=JewelSphy <onboarding@resend.dev>
